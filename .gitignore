# Python cache
__pycache__/
*.py[cod]

# Virtual environment
.venv/

# Streamlit secrets (never commit these)
.streamlit/secrets.toml

# User data (regenerated at runtime, not part of source)
data/transactions.csv
data/budgets.json
data/recurring.json
data/backup/

# OS noise
.DS_Store
Thumbs.db